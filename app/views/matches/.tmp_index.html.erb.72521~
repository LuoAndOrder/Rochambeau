<h2>All Matches</h2>

<% if !@matches.empty? %>
  <ul class="matches_list">
  <% @matches.each do |match| %>
    <li>
      <% link_to(:controller => "matches", :action => "show",
                 :id => match.id) do %>
      	"<%= h match.match_name %>" <span class="creator_name">by <%= h match.player_1 %></span>
    	<% end %>
  	</li>
  <% end%>
  </ul>
<% else %>
  <p>There are no matches yet.</p>
<% end%>